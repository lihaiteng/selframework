<?xml version= "1.0" encoding ="utf-8"?>
<!--可以设置的形状:rectangle：矩形, 默认为矩形;oval：椭圆;line:线;ring：环形；-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle"
    >

    <!-- corners : 弧度 shape为矩形时才有效-->
    <corners android:radius="16dp"/>

    <!--  size ： 设置大小 最终还是会根据所在控件大小展示 -->
    <size
        android:width="200dp"
        android:height="32dp"
        />

    <!-- solid : 颜色 -->
    <solid android:color="#993bc4c9"/>

    <!-- 颜色渐变 -->
     <gradient android:startColor="#ff0000"  android:centerColor="#00ff00"  android:endColor="#0000ff" android:angle="90"/>

    <!-- 内部间距 -->
    <padding
        android:bottom="10dp"
        android:left="30dp"
        android:right="30dp"
        android:top="10dp"/>

    <!-- 设置边框 dashWidth : 虚线的长度   dashGap:虚线之间的距离-->
    <stroke
        android:width="5dp"
        android:color="#de151f"
        android:dashWidth="10dp"
        android:dashGap="20dp"/>

</shape>